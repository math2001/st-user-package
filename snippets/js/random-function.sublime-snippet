<snippet>
    <content><![CDATA[function rnd(min, max, integer) {
	if (!integer) {
		return Math.random() * (max - min) + min;
	} else {
		return Math.floor(Math.random() * (max - min + 1) + min);
	}
}]]></content>
    <tabTrigger>randomfunction</tabTrigger>
    <scope>source.js</scope>
    <description>Create random function</description>
</snippet>
